# ModiCube
ModiCube, the custom [ClassiCube](https://github.com/ClassiCube/ClassiCube) client.

# Building
1. Clone the Repository
2. Run `make <platform> RELEASE=1`
3. Run the `ClassiCube` executable
